{
  "name": "chult-map-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.5.0",
  "engines": {
    "node": ">=18.20.4"
  },
  "scripts": {
    "dev": "pnpm server:dev",
    "build": "pnpm server:build",
    "start": "pnpm server:start",
    "format": "pnpm run format:root && pnpm client:format && pnpm server:format",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r test:coverage",
    "test:watch": "pnpm -r test:watch",
    "server:dev": "pnpm --filter chult-map-service dev",
    "server:build": "pnpm --filter chult-map-service build",
    "server:start": "pnpm --filter chult-map-service start",
    "server:format": "pnpm --filter chult-map-service format",
    "server:test": "pnpm --filter chult-map-service test",
    "server:test:coverage": "pnpm --filter chult-map-service test:coverage",
    "server:test:watch": "pnpm --filter chult-map-service test:watch",
    "format:root": "prettier --write \"./*.{js,ts,tsx,jsx,json,md}\" ./shared --ignore-path .prettierignore",
    "client:format": "pnpm --filter chult-map-client format",
    "client:test": "pnpm --filter chult-map-client test",
    "client:test:coverage": "pnpm --filter chult-map-client test:coverage",
    "client:test:watch": "pnpm --filter chult-map-client test:watch"
  },
  "devDependencies": {
    "prettier": "^3.7.4"
  }
}
