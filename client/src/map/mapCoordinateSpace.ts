// TODO: Refactor hex data to carry authoritative coordinate-space metadata,
// then derive these values from that source instead of hard-coding here.
export const MAP_VIEWBOX_WIDTH = 4476;
export const MAP_VIEWBOX_HEIGHT = 6000;
export const MAP_ASPECT_RATIO = MAP_VIEWBOX_WIDTH / MAP_VIEWBOX_HEIGHT;
